#include "main.h"
/**
* _abs - to compute the absolute value of an integer
*@i: targeted number
*Return - Always 0
*/
int _abs(int i)
{
if (i > 0)
{
return (i);
}
else if (i < 0)
{
return (-i);
}
else
{
return (0);
}
}
